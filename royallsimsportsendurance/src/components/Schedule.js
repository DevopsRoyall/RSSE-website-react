import { Container } from 'react-bootstrap';
import ScheduleTable from './ScheduleTable';

const Schedule = () => {
  return (
    <Container>
      <h2>Schedule</h2>
      <ScheduleTable />
    </Container>
  );
};

export default Schedule;
